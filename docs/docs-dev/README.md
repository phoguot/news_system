# docs-dev — Khung tài liệu chung cho mọi dự án

> Mục tiêu: dự án sau chỉ cần copy thư mục này sang là dùng ngay — không phụ thuộc dự án cụ thể.
> Mỗi file là **mẫu (template)** có sẵn tiêu đề + gợi ý điền. Điền vào, không cần tạo mới.

> **Trạng thái (12/09/2026):** đã điền toàn bộ nội dung cho **Van Lang — News CMS**. Sự thật nghiệp vụ/DB gốc: [`../phan-tich-he-thong-website-tin-tuc.md`](../phan-tich-he-thong-website-tin-tuc.md) (v1.5). Luật đọc cho AI agent ở **`AGENTS.md` gốc repo**: ưu tiên **skill > docs > code** — skill dẫn quy trình làm việc, docs dẫn chuẩn/thực tế, code xác minh cuối cùng; khi docs mô tả trạng thái lệch code/schema thật thì **báo user + cập nhật docs lại trong cùng phiên**, không âm thầm theo một bên. Copy sang dự án mới: xóa nội dung các file đã đánh số, giữ nguyên cấu trúc + `_templates/`.
> **Mốc 12/09/2026:** xác thực admin (login/logout + guard `/admin` 302 & `/api/admin` 401 + khoá 5 lần sai) và **form liên hệ công khai** (CSRF + captcha + honeypot + rate-limit 429 + mail notify không chặn luồng) **đã code xong, smoke-test HTTP + phpunit/phpcs/psalm pass** — chi tiết [`05-van-hanh/03-xac-thuc-phan-quyen.md`](05-van-hanh/03-xac-thuc-phan-quyen.md).
> **Mốc 12/09/2026 (cấu trúc module):** `module/` tổ chức lại theo chuẩn EnglishTrain-style — `Model/<Entity>/{Mapper,Const}` thay `Table/`, `Filter/` (InputFilter) thay `Form/`, guard vào `Service/`, DI closure thay file `*Factory.php`. **Refactor đã thực hiện xong 12/09/2026** cho toàn bộ code hiện có (DI dùng chung, Admin auth, Frontend contact) — phpunit 18/18, phpcs sạch trên các file touched. Nguồn sự thật: [`01-quy-chuan/07-crud-convention.md`](01-quy-chuan/07-crud-convention.md); cây thư mục + trạng thái refactor: [`00-tong-quan/05-cau-truc-thu-muc.md`](00-tong-quan/05-cau-truc-thu-muc.md). Luật agent: `AGENTS.md` gốc repo (đọc skill > docs > code). Việc 🟡 còn lại: `CLAUDE.md` từng module, CRUD các resource chưa triển khai (dashboard/account), kéo thả thứ tự + chọn mục manual home-section (FR-32/FR-33), cache settings (FR-39), xuất hộp thư (FR-35).
> **Mốc 12/09/2026 (CRUD nội dung + API):** **categories · tags · posts đã code thật** đủ tầng `Controller → Service → Model/Mapper → Filter → View` (state machine bài viết nháp/xuất bản/lưu trữ + hẹn giờ, revision giữ 20 bản, cắt revision ngay sau ghi, xoá cứng 1 transaction dọn bảng con, slug không dấu + purifier + reading-time) + API `/api/admin/*` dispatcher đúng envelope [`01-quy-chuan/05-quy-chuan-api.md`](01-quy-chuan/05-quy-chuan-api.md) (401/404/409/422, camelCase, ISO 8601 UTC; reorder/bulk/autosave/revisions chưa làm → 404 như route lạ). Nền tảng sinh ra dùng chung: `Application/Constant/ContentConst`, `Application/Service/{SlugService,HtmlPurifierService}`, `DbService::transactional()` (trước 13/09 thuộc `Core\`), `Admin/Exception/*` (Validation/NotFound/Conflict) + 45 unit tests. Nghiệm thu: phpunit **63/63**, phpcs sạch trên file đụng vào, psalm **137 ≤ baseline 162**, DI smoke controller/service resolve OK. Cây thư mục + trạng thái: [`00-tong-quan/05-cau-truc-thu-muc.md`](00-tong-quan/05-cau-truc-thu-muc.md) §2–3.
> **Mốc 12/09/2026 (chuẩn luồng — thay thế cách đặt Filter ở Controller của các mốc trên):** toàn bộ code thật chạy theo luồng **Controller (mỏng: chỉ nhận request + inject id route vào raw, gọi Service, render) → Service (`new` InputFilter validate từng request, bắt `ValidationException` → field errors / PRG flag) → Mapper (method đọc trả `<Entity>Model` hydrate qua `fromRow()`; `toArray()` cho API, `toFormValues()` cho form refill) → Service trang trí (`categoryName`/`postCount`…) → View/JsonModel**. Áp dụng cho Admin Post/Category/Tag/Auth/Api + Frontend Contact; csrf hash expose qua Service (`saveFormCsrfHash`/`actionCsrfHash`/`loginCsrfHash`…), luồng API dùng `$withCsrf = false`. Chuẩn thức chi tiết: [`01-quy-chuan/07-crud-convention.md`](01-quy-chuan/07-crud-convention.md) §2–3, §6, §8. Nghiệm thu: phpunit **67/67**, phpcs sạch trên file đụng vào, psalm **141 ≤ baseline 162**.
> **Mốc 12/09/2026 (batch CRUD services · banners · team):** ba resource viết mới **đủ tầng theo chuẩn luồng trên** — `Model/<Entity>/{Mapper,Model,Const}` (đọc hydrate `fromRow()`, `toArray()` giữ cho API sau này), `Filter/<Entity>/{Save,Action}Filter` chạy trong Service, Controller mỏng + PRG flag, view `index+form` thật. Điểm riêng: **`services` do `Frontend\Model\Service\ServiceMapper` sở hữu** — mapper này vừa đọc trang khách vừa ghi hộ CRUD admin (container gộp, không tạo mapper thứ hai; xoá bị chặn khi còn `contact_submissions` tham chiếu `serviceId`); **banner** có lịch hiển thị `startAt/endAt` nhập giờ VN → lưu UTC (quy tắc docs §4.1) + chặn end ≤ start; **team** giữ nguyên `userId`/`socialLinks` (chưa có UI). Nghiệm thu: phpunit **96/96**, phpcs sạch trên file đụng vào, psalm **129 ≤ baseline 162**.
> **Mốc 13/09/2026 (batch CRUD contacts · settings · home-sections):** ba resource admin còn lại viết **đủ tầng theo chuẩn luồng trên** — hộp thư (FR-35): lọc status/dịch vụ/từ-đến ngày (ngày VN → biên UTC, lọc lỏng), `updateHandler` tự đóng `handledAt` khi "Đã xong", spam = status 3, xoá; **KHÔNG có create** (ghi của khách do `Frontend\ContactService`). Cài đặt (FR-39): một form phẳng `setting_<id>` cho 4 nhóm, validate theo `valueType` (number/boolean/media/json), **chỉ ghi dòng thực sự đổi** + `updatedBy` từ identity. Bố cục trang chủ (FR-32): `HomeSectionConst::TYPE_LABELS` mã 1–7, `config` phải là JSON object với khóa/ràng buộc **theo từng type** (mode auto|manual, limit 1–50, hero interval_ms, category_posts yêu cầu `category_id` TỒN TẠI qua CategoryMapper, CTA button_url), xoá section **cascade dọn `home_section_items` trong một transaction** (`DbService::transactional`). Ghi hộp thư + settings đi qua `ContactMapper`/`SettingMapper` **của Frontend** (1 bảng 1 mapper — 05-cau-truc §4). Việc 🟡 còn của batch này: kéo thủ công thứ tự thay vì ô số (FR-32), chọn mục `mode=manual` (FR-33), cache settings (FR-39), xuất (FR-35). Nghiệm thu: phpunit **129/129**, phpcs sạch trên file đụng vào, psalm **115 ≤ baseline 162** (dọn luôn một lỗi tồn dư của batch trước), DI smoke OK.
> **Mốc 13/09/2026 (batch thư viện media — FR-36/37/38):** thư viện ảnh admin viết **đủ tầng theo chuẩn luồng trên** — `Model/Media/{MediaMapper,MediaModel,MediaConst}` + `Filter/Media/{MediaUpload,MediaAlt,MediaAction}Filter` chạy trong `MediaService` + `MediaController` mỏng + view `index/edit/usages` thật. Upload nhiều file: cổng an toàn `finfo` MIME (danh sách trắng `app.allowed_mimes` — từ chối php giả png, chặn svg), kiểm `app.upload_max_mb` **trước** khi ghi đĩa, tên ngẫu nhiên `YYYY/MM/<hex32>.<ext>` trong `public/uploads` (extension suy từ MIME, không tin tên gốc), biến thể WebP `thumb/medium/large` sinh bằng GD chỉ hạ kích thước (gif giữ nguyên file). Sửa `altText` form riêng; màn `usages` gom id từ 8 mapper chủ bảng (post quét thêm ảnh nhúng trong `content` LIKE; setting theo `valueType` media); **xoá chặn khi còn tham chiếu** + kiểm đủ file trên đĩa trước khi `unlink` rồi mới xoá dòng. Lưu ý kỹ thuật rút từ batch này: `Select::where()` của laminas-db **không nhận mảng params ở đối số hai** — muốn OR/LIKE an toàn phải dựng đối tượng `Where` (`equalTo` → `->or->equalTo`, `like`) rồi truyền vào `where()`; giá trị vẫn bind qua `ValueBinder` nên prepared statement giữ nguyên. Nghiệm thu: phpunit **143/143** (+14 test media), phpcs sạch trên file đụng vào, psalm **112 ≤ baseline 162**, DI smoke `MediaController`/`MediaService`/`MediaMapper` resolve OK.
> **Mốc 13/09/2026 (dời Filter dùng chung về Application):** `module/Application/src/{Filter,Factory}` (7 file thêm ngoài chuẩn — `AppFilter`, `AppServiceFactory`, `HTMLPurifierFactory`…) được dọn: xóa 5 file vi phạm cấm `*Factory.php`/`AppFilter` base (AGENTS.md §2, 07 §4), dời 2 file có giá trị về `Application/src/Filter/` — `CommonFieldFilters` (bỏ import gãy `BusinessFileTypeMap`, hằng `LEN_*` giữ làm config helper) + `HtmlPurifierFilter` (bỏ `VENDOR_PATH/BASE_PATH`, cache dir qua `__DIR__`) — đúng chuẩn "dùng chung → Application" (07 §7, 05-cau-truc §2) (trước mốc này các file thuộc `Core\`); `Application` trở lại skeleton + dịch vụ dùng chung. 24 Filter hiện tại giữ nguyên, code mới có thể `use Application\Filter\CommonFieldFilters::textField(...)` dần.
> **Mốc 13/09/2026 (áp dụng pattern webapp-be — chỉ phần phù hợp, đối chiếu [`01-quy-chuan/08-vi-du-sai-dung.md`](01-quy-chuan/08-vi-du-sai-dung.md) §5):** bốn khoảng trống 🟡 cuối được đóng: (1) `Application/Service/DateService` — helper tĩnh mốc giờ UTC tập trung (trước 13/09 thuộc `Core\`) (`nowUtc`/`timestampUtc`/`plusMinutesUtc`/`isoToUtc` lenient), thay `gmdate`/`time()` rải rác ở `PostService`, `AdminAuthService` (khoá tài khoản giữ nguyên luật 5 lần/15 phút), `Frontend\ContactService` (`consentAtUtc`); `PostService::isoToUtc/nowUtc` còn lại làm wrapper giữ API cũ cho test/CLI. (2) `Admin/Service/Api/{ApiResultModel,ApiResponseModel}` — **luồng API JSON: Service trả trọn envelope `{success,data,meta,errors}` + HTTP status** (`ok`/`error`/`fromThrowable` map Validation→422 · Conflict→409 · NotFound→404; `serializeModel/serializeRow` `*At`→ISO `…Z`); `ApiController` còn router thuần (parse route → gọi `listApi/readApi/saveApi/deleteApi/mergeApi/countsApi/publishApi/archiveApi/draftApi/previewTokenApi` của Category/Tag/PostService → gắn status); `buildTree` categories + lọc `q` tags dời vào Service tương ứng. (3) Hàm update 1–2 cột chuyên biệt theo 08 §3: `PostMapper::updatePublished/updateArchived/updateDrafted/updatePreviewToken`, `UserMapper::updatePasswordHash`, `MediaMapper::updateAltText` (xoá `MediaMapper::update` chung vì hết caller). (4) Bỏ chuỗi mệnh đề SQL theo 07 §5.1 toàn bộ `Category/Post/Media/User` Mapper. **Không áp dụng** (không phù hợp hệ News): businessId/multi-tenant, scoped filter bases (`canWebApp`…), DI `AppInvokableFactory` + `Module::getServiceConfig` (trái chuẩn closure của 07 §4 — docs thắng; **hoàn tác cùng ngày 13/09/2026**: user duyệt `AppServiceFactory`/`AppInvokableFactory` làm nền tầng Service — xem milestone "DI nền" cuối file + 07 §4 bản mới), cursor `LastIdPaginator`, cơ chế `extraContent`, `CommonFieldFilters` (gộp 24 filter → batch riêng nếu muốn). Nghiệm thu: phpunit **164/164** (+5 test DateService), phpcs sạch trên file đụng vào, psalm **103** (giảm 9 so với mốc 112 cùng ngày).
> **Mốc 13/09/2026 (gộp `module/Core` về `module/Application`):** 13 file `Core\src\*` dời sang `Application\src\*` và đổi namespace `Core\` → `Application\` (DbService, SlugService, HtmlPurifierService, MailService, CaptchaService, DateService, CommonFieldFilters, HtmlPurifierFilter, ContentConst, SessionBootstrap, MediaUrl…); `module/Application/config/module.config.php` gộp `service_manager` + `view_helpers` (aliases `DbAdapter`/`mediaUrl`); `composer.json` PSR-4 + `config/modules.config.php` xóa `Core`; `module/Admin` + `module/Frontend` đổi hết `use Core\` → `use Application\`. Toàn bộ docs `docs-dev/` đồng bộ trong cùng ngày.
> **Mốc 13/09/2026 (nền tảng Filter — `AppInputFilter`):** theo yêu cầu user, tạo `Application\Filter\AppInputFilter` và chuyển **toàn bộ 25 filter** (Admin 24 · Frontend 1) từ `extends InputFilter` sang `extends AppInputFilter`: CSRF (validator `Csrf` name `csrf` + input tự add theo `$withCsrf`), `csrfHash()`, `fieldErrors()`, `setContainer`/`getContainer`, helper khai báo field (`addIdField`/`addStringField`/`addSlugField`/`addIntCastField`/`addRawField`/`addDateTimeField`) và getter an toàn kiểu (`positiveIdValue`/`stringValue`/`nullableStringValue`/`flagValue`) gom một mối; filter riêng chỉ khai báo field rồi `parent::__construct($withCsrf)` cuối constructor (quên là fatal — readonly chưa init). Luật cũ giữ nguyên: Service `new` mỗi request, cấm đăng ký filter vào container, API JSON `$withCsrf = false` + SameSite=Lax. Chuẩn mới: 07 §6 (viết lại), 05-cau-truc §2/§3, AGENTS.md §2–§3. Nghiệm thu: phpunit **164/164**, phpcs sạch trên file đụng vào, psalm **103** (`--stats`, 0 lỗi thuộc nhóm file filter — dọn 2 UnusedPsalmSuppress tồn dư), smoke `/admin/login` + `/lien-he` HTTP 200 kèm đúng 1 hidden input `csrf`. **Cùng ngày sửa lỗi tồn batch service-locator:** 6 service Admin (`AdminAuthService`, `CategoryService`, `BannerService`, `TeamMemberService`, `SettingService`, `AccountService`) đã kế thừa `AppServiceFactory` nhưng `module/Admin/config/module.config.php` vẫn đăng ký closure constructor-cũ (PHP bỏ qua thừa số thừa → container không được mồi → 500 trên `/admin/login`); đổi sang `AppInvokableFactory::class` như `TagService`, cập nhật 5 test tương ứng sang `setContainer(new TestContainer(...))`, chạy lại `composer dump-autoload` (classmap cũ che hỏng); psalm từ 115 xuống 103 nhờ xoá các closure đăng ký.
> **Mốc 13/09/2026 (batch 5 — Account FR-14 · Dashboard §3.1 · DI nền cho toàn bộ Service):** (1) `Admin\Service\AccountService` + `AccountController` + `Filter/Account/{Profile,Password}Filter` + view "Tài khoản của tôi" (CSRF qua filter, đổi mật khẩu ghi `UserMapper::updatePasswordHash`, `refreshIdentity` sau đổi email/tên); `DashboardController`/`DashboardService` tổng hợp số liệu qua 4 mapper (không JOIN chéo); sửa bug tồn: `PostMapper::listFiltered` mệnh đề `:q` chuỗi không được `prepareStatementForSqlObject` bind → chuyển sang object `Where` (07 §5.1). (2) **DI nền theo yêu cầu user**: `Application\Factory\AppServiceFactory` (getContainerEntry/setContainer) + `AppInvokableFactory` là 2 file nền được duyệt trong `Application/src/Factory/` — **toàn bộ 15 service** (Admin 14 + Frontend `ContactService`) bỏ constructor injection, chuyển sang `extends AppServiceFactory` với typed accessor `getContainerEntry(...)`; 8 đăng ký closure còn lại đổi sang `AppInvokableFactory::class`; 14 test service dùng `(new X())->setContainer(new TestContainer([...]))`. Controller/Mapper giữ closure; `DbService`/`MailService` (nhận mảng config) giữ closure — ngoại lệ ghi trong 07 §4. (3) Sửa bug latent: `MediaService::appConfig()` đọc `ApplicationConfig` (chỉ có `modules`/`module_listener_options`, không hề có khối `app` — cả closure cũ cũng luôn nhận `[]`); chuyển đọc entry `Config` merge → `upload_dir/upload_max_mb/allowed_mimes/image_variants` trong global.php nay có hiệu lực thật. Cập nhật chuẩn: 07 §4 (viết lại), 08 §5 (2 dòng mới), 05-cau-truc, AGENTS.md §2. Nghiệm thu: phpunit **164/164**, phpcs sạch trên file đụng vào, psalm **99 ≤ baseline 140/162** (giảm nhờ xoá closure đăng ký + suppress chết), DI smoke 13 service + 2 controller resolve OK kèm accessor `Router`/`Config`.
> **Mốc 13/09/2026 (batch 6 — FR-33 quản lý mục cho section `mode=manual`):** Block quản mục trong trang `/admin/home-sections/edit/:id` — thêm/xoá/lên/xuống **server-side theo form PRG** (chưa kéo thả JS thật; thứ tự hiển thị qua nút ↑↓ + `sortOrder` số). File mới: `Model/HomeSectionItem/HomeSectionItemModel.php` (POPO `fromRow`), `Filter/HomeSection/HomeSectionItemsFilter.php` (`AppInputFilter` — CSRF + `op` ∈ add/remove/up/down + `id`|`itemType`,`itemId`); `HomeSectionConst` thêm 6 flag `FLAG_ITEM_*`, `ITEM_LIMIT`(=50), `MANUAL_ITEM_TYPES` (featured_posts→post · services→service · team→team_member); `ContentConst::SECTION_ITEM_LABELS` (nhãn render 2 module). `HomeSectionItemMapper` thêm `listBySectionId` (hydrate theo `sortOrder`,`id`), `insert`, `existsItem` (chặn trùng UNIQUE `uq_home_section_items`), `deleteByIdAndSection` (guard sở hữu theo section), `updateSortOrder`; `HomeSectionService::itemsForm(sectionId, raw): string` một mối validate → nhánh `op`: itemType phải khớp loại section, bảng chủ kiểm tồn tại qua mapper chủ (đa hình §4.4.4 — không JOIN), trùng → `item-duplicate`, vượt trần 50 → `item-limit`, thêm gắn cuối (`max+1`), lên/xuống swap + renumber `0..n-1` trong MỘT `DbService::transactional`; mọi thao tác resolve dòng qua `findItemInSection` (không tin `id` từ raw một mình). `HomeSectionController::itemsAction` (route `[/:action[/:id]]` có sẵn — không đổi) mỏng + `renderForm` supply `items/manualMode/allowedItemType/itemLabels/itemsCsrfHash`; `form.phtml` FR-33 block + map flag→alert (hằng trỏ FQCN vì phtml không import). Nghiệm thu: phpunit **184/184** (FR-33 +11; song song worker cùng phiên migrate Setting/Contact + PageCache tồn dư, test đã gộp xanh — theo dõi xung đột như các batch trước), phpcs sạch file đụng vào, psalm **100** (file batch 6 chỉ còn đúng 2 lỗi có từ trước trong `HomeSectionController::queryFlag`/`HomeSectionService::create`; mức nhích so với 99 của batch 5 do thay đổi song song của worker, ghi nhận ở đây), DI smoke controller+service+3 mapper chủ resolve OK.
> **Mốc 13/09/2026 (batch 7 — nền DI phủ 19/19 service, bỏ nốt ngoại lệ Application):** theo yêu cầu user ("không dùng constructor injection, gọi `$this->getContainerEntry(...)` như demo, mọi service viết tương tự"), 4 service cuối còn constructor trong `Application\Service` chuyển về nền: `DbService` (key `db` — adapter tạo LAZY lần gọi `getAdapter()` đầu, giữ alias `DbAdapter`, `transactional()` đi qua lazy adapter), `MailService` (`mail`), `CaptchaService` (`recaptcha` — `recaptchaConfig()` ép 3 field string đúng mặc định cũ), `HtmlPurifierService` (bỏ tham số `cacheDir` chưa từng được truyền khác mặc định; property đổi tên `$engine` vì nền `AppServiceFactory` đã chiếm tên `$purifier` — xung đột visibility fatal). Tất cả đọc `Config` merge qua accessor riêng theo khuôn `MediaService::appConfig()`; `module/Application/config/module.config.php` đổi 4 closure dài → `Factory\AppInvokableFactory::class` (xoá import `ContainerInterface` hết dùng). **Phát hiện quan trọng:** `config_cache_enabled => true` làm container vẫn chạy closure CŨ đã cache sau khi đổi đăng ký (`new DbService($db)` thừa số bị PHP bỏ → instance không container → fatal `getContainer()`) — phải `php bin/clear-config-cache.php` sau mọi sửa `module.config.php`; đã ghi cảnh báo vào 07 §4 + AGENTS §2. Cập nhật chuẩn: 07 §4 (bullet "không còn ngoại lệ"), 05-cau-truc (cây Service Application + dòng §3 "19/19"), AGENTS §2. Nghiệm thu: phpunit **186/186**, phpcs sạch file đụng vào, psalm **98** (< 99 của batch 5 — 4 file mới 0 lỗi, các flag UnnecessaryVarAnnotation/MixedReturn đã sửa theo đúng shape `appConfig`), smoke CLI `Application::init`: `DbService::getAdapter()` = `Laminas\Db\Adapter\Adapter` từ Config, alias OK, captcha dev tắt → `verify(null)=true`, `sendMany([]) = false`, `purify` bóc `<script>`; HTTP `php -S` sau clear cache: `/` `/lien-he` `/admin/login` `/tin-tuc` = 200.
> **Mốc 13/09/2026 (batch 8 — FR-39 cache settings + nền page_cache):** **Sửa bug latent**: `caches.page_cache` trong `global.php` khai báo schema **v2** (`adapter.name` lồng) trong khi composer chạy laminas-cache **3.14** — `StorageAdapterFactory` sẽ từ chối cấu trúc khi dựng service (chưa nổ chỉ vì chưa ai resolve `page_cache`); chuyển về **schema v3** (`adapter` = chuỗi class Filesystem, `options` top-level `cache_dir`+`ttl=60`, plugin `ExceptionHandler throw_exceptions=false`). Nền cache mới **`Application\Service\PageCacheService`** (extends AppServiceFactory, đăng ký `AppInvokableFactory`): `remember(key, producer)` / `forget(key)`; **không dùng plugin `serializer`** (cần package `laminas-serializer` chưa cài — smoke fatal ra) mà tự `serialize` payload **bọc mảng 1 phần tử** để phân biệt giá trị `false`/`null` đã cache với miss, guard prefix `a:1:` chống dữ liệu rác; doctrine "cache là tăng tốc": storage thiếu/không dựng được → producer chạy thẳng. **`Application\Constant\CacheConst`** = tên service + key (`settings-v1`; dành chỗ `home-v1`). Tầng đọc settings công khai **`Frontend\Service\SettingService`**: map `settingKey=>settingValue` cache TTL 60s + `stringOrNull()` + `invalidate()`. `Frontend\ContactService` đọc notify-email qua SettingService thay `SettingMapper::getValue` — method hết caller nên **xoá** (precedent `MediaMapper::update`); `Admin\SettingService::saveForm()` invalidate sau mọi lần ghi (reuse chéo module qua container gộp — 05-cau-truc §4). Filesystem adapter **không tự tạo `cache_dir`** → `data/cache/page/.gitkeep` vào repo (gitignore re-include 3 dòng). Cache dữ liệu trang chủ 🟡 để batch homepage (`HomeController` còn placeholder, ghi rõ trong docs §Cache). Nghiệm thu: phpunit **186/186** (batch này +12 test — 7 PageCacheService, 5 Frontend SettingService; rewiring ContactServiceTest + Admin SettingServiceTest với invalidate), phpcs sạch file đụng vào, psalm **98 ≤ 99** (chạy `--no-cache` làm chuẩn — cached run đếm lệch; giảm 2 nhờ xoá getValue), DI smoke container thật resolve 2 service mới + vòng miss→hit (producer đúng 1 lần)→forget→tính lại, `ttl=60` đọc từ config. Ghi chú cộng tác: chạy song song batch 7 của worker trên cùng repo — milestone 7 đã gộp sẵn số 186/98; docs §Cache (03-kien-truc), cây thư mục + narrative (05-cau-truc), AGENTS §2/§4 đồng bộ cùng phiên.
> **Mốc 13/09/2026 (đăng nhập bằng username):** `users` thêm cột `username VARCHAR(50) NULL` + `UNIQUE uq_users_username` (incremental `data/schema/2026-09-13-them-cot-username.sql`, backfill `admin` cho dòng hiện hữu). Form login còn **1 ô định danh** (`LoginFilter` field `identity`, regex tập ký tự an toàn — bỏ validator `EmailAddress` vì chặn username); `UserMapper::getUserByEmail` → **`getUserByIdentifier`** (chứa `@` → tra email, không → tra username); `AdminAuthService::login(string $identifier, …)` giữ nguyên thứ tự khoá→verify và thông điệp chống dò chung ("Email, tên đăng nhập hoặc mật khẩu không đúng."). Identity session mở rộng `{id, email, fullName, username}`; `refreshIdentity` thêm tham số username. Trang "Tài khoản của tôi" cho đổi username: `AccountProfileFilter` nhận `?callable $usernameAvailable` (Service inject closure `UserMapper::isUsernameTaken`, pattern `UserConst::USERNAME_PATTERN` 3–32 ký tự thường không dấu — collation `utf8mb4_0900_ai_ci` không phân biệt hoa/thường/dấu nên chặn bằng regex từ input; rỗng = NULL, chỉ đăng nhập email). `bin/create-admin.php` thêm `--username=` optional. **Cùng phiên sửa bug latent:** 35 dòng closure `static fn … $this->…` trong `module/Admin/view/**/*.phtml` (22 file) gây fatal "Using $this when not in object context" trên MỌI trang admin đã đăng nhập (dashboard/posts/account…) — các mốc trước chỉ smoke trang public + login nên chưa phát hiện; bỏ `static`. Nghiệm thu: phpunit **204/204** (batch này +8 test), phpcs sạch file đụng vào, psalm **118 --no-cache** (batch này 0 lỗi mới trên file chạm; lưu ý lệch +20 so với mốc 98 của batch 8 do thay đổi ngoài phiên — cần rà lại), HTTP smoke: login bằng `admin` và `admin@vanlang.vn` đều 302 dashboard, `/admin/account` đổi username → login bằng username mới OK → revert, sai định danh trả đúng thông điệp chung.
> **Mốc 13/09/2026 (batch 9 — FR-32 render trang chủ + FR-39 cache `home-v1`):** `HomeController`/`Frontend\Service\HomeService`/`view/frontend/home/index.phtml` từ placeholder thành **code thật**: trang chủ dựng đủ 7 loại section theo luồng §8 (`HomeSectionMapper::listActiveOrdered` → mỗi type gọi **method đọc công khai mới trên mapper chủ bảng** — `PostMapper::listLatestPublished/listFeaturedPublished/listPublishedByCategory/listPublishedByIds` với scope `status=PUBLISHED AND publishedAt<=UTC_TIMESTAMP()`, `BannerMapper::listActiveHomeHero` kèm cửa sổ `[startAt,endAt]` (nested `Where` isNull/or), `ServiceMapper::listActive/listActiveByIds`, `TeamMemberMapper::listFeaturedActive/listActiveByIds`); `mode=manual` giữ đúng thứ tự `home_section_items`, mục chết tự bỏ qua; heading trống fallback tên category; ảnh mọi card resolve **một** query `MediaMapper::mapCardsByIds` (thumb ưu tiên biến thể — chống N+1). Payload **mảng thuần** cache nguyên khối `remember(CacheConst::KEY_HOME)` TTL 60s; **5 service ghi** Admin (Post/Banner/Service/TeamMember/HomeSection-cả-itemsForm) `forget('home-v1')` sau mọi write — PageCacheService là dependency mềm (accessor instanceof-guard) nên 0 test cũ phải rewire. Slider hero `public/assets/js/home-slider.js` (autoplay + dots). Bug bắt khi viết test: block category nhầm sang featured — sửa bằng tham số `categoryId`. Precedent mới ghi §4: **Frontend đọc mapper Admin-owned** qua container gộp, chỉ SELECT. Nghiệm thu: phpunit **204/204** (+11 test batch 9 — 6 HomeService + 5 forget-cache; gộp +8 username của milestone trước), phpcs sạch, psalm **90** `--no-cache` (< mốc 98 batch 8; con số 118 ở milestone trước chính là file batch 9 dở dang — đã rà xong đúng lời "cần rà lại"), HTTP smoke `php -S`: `/` 200 miss→ghi file `laminascache-home-v1.dat`→hit giống hệt→`forget` gỡ file→GET rebuild OK. Ghi chú: cột "Trạng thái" bảng FR (`02-thiet-ke/01-yeu-cau-chuc-nang.md`) trễ toàn bảng so với repo — mới lật FR-01/FR-32 thuộc batch này, cần rà full ở batch riêng.
> **Mốc 13/09/2026 (rà toàn bảng trạng thái FR — đóng lời "cần rà full" ở milestone trên):** cột "Trạng thái" của cả 42 dòng FR trong `02-thiet-ke/01-yeu-cau-chuc-nang.md` được đối chiếu **code thật từng file** (agent khảo sát riêng, evidence file:line) và lật lại đúng hiện trạng — kết quả: **20 ✅** (FR-01/10/12/14/16/17/18/20/23/24/27/29/30/33/36/37/38/39/40/42), **10 🟡** ghi rõ phần còn ngay trong ô (FR-04 previewToken FE+noindex · FR-15 lọc ngày · FR-19 fallback biến thể thumbnail · FR-21 autosave/compare/restore · FR-26/29·30 kéo thả reorder · FR-28 autocomplete tag · FR-31 position ngoài hero · FR-32 kéo thả section · FR-34 socialLinks+userId · FR-35 xuất), **4 ❌** chưa có code (FR-13 reset mật khẩu · FR-22 bulk · FR-25 bài liên quan · FR-41 ghi lượt xem), **8 Khung** đúng định nghĩa legend (FR-02…09 + FR-11 — controller stub trả view 1 thẻ `<h1>`). Tổng 42 dòng khớp. Legend đầu file bổ sung ký hiệu ✅/🟡/❌ + ghi ngày rà. Phát hiện đáng chú ý ngoài lề: `MediaUrl` nhận `$variant` nhưng bỏ qua (liên quan FR-19); hằng banner `news_top` có vị trí chưa nơi render (FR-31). Không sửa code trong lần rà này — bảng chỉ phản ánh hiện trạng; các 🟡/❌ là backlog có chủ đích.
> **Mốc 13/09/2026 (đăng nhập theo mockup + launcher + seed admin + sửa luồng LƯU BÀI/LƯU NHÁP):** (1) `auth/login.phtml` dựng lại theo mockup user cập nhật `Image/files/cms-login.html` (2 panel tối/sáng, 1 ô định danh, alert server-side + `csrfHash()`, JS chỉ còn toggle mật khẩu/spinner submit — bỏ mọi fake demo login/SSO); `run-local.bat` ở root repo: clear-config-cache → `php -S 127.0.0.1:8000 -t public` → tự mở browser vào **`/admin/login`** (8080 đụng app khác trên máy user; file giữ CRLF). (2) Seed admin dev nhanh `data/schema/2026-09-13-seed-admin.sql` — upsert idempotent đúng luật 1-dòng `users`, password dev công khai `Admin@1234`, production vẫn bắt buộc `bin/create-admin.php`. (3) **Sửa "hệ thống không có lưu nháp"** — mọi lượt lưu bài 500 trên driver **Pdo** vì 2 lỗi tầng: `DbService::transactional()` gọi `transientBegin/Commit/Rollback` (chỉ có ở driver Mysqli → chuyển `beginTransaction/commit/rollback` thuộc ConnectionInterface; test không bắt được vì DbService mock), và `PostRevisionMapper` dùng mảng-tắt `'type !=' =>` mà laminas-db 2.22 sinh SQL hỏng trên MySQL platform (1064) → `Where` + `Predicate\Operator` tường minh, align luôn file theo 07 §5.1 — **luật mới ghi 07 §5.1**. Nới semantics nháp theo chuẩn mới 07 §6.2: `PostSaveFilter` thêm `bool $draftMode` (title bỏ min 3, content được trống; `categoryId` vẫn bắt buộc vì DB NOT NULL; message lỗi tiếng Việt), `PostService::saveValidated()` bật draftMode **chỉ khi** `intent=draft` VÀ bài mới hoặc đang status NHÁP (bài đã xuất bản không bị xoá trắng qua ô nháp), nút "Lưu nháp" gắn `formnovalidate`. Kèm: sửa `layout/admin.phtml` gọi `$this->getRequest()` trong view ( PhpRenderer không có → `ServiceNotFoundException "A plugin by the name getRequest"`) → helper `serverUrl(true)` + `parse_url` đúng như milestone giao diện đã mô tả. Nghiệm thu: phpunit **221/221** (số test nhảy ngoài phiên — repo chỉnh song song), phpcs sạch file đụng vào, psalm **82 `--no-cache`** (< mốc 90 batch 9: sửa mapper + dọn ngoài phiên), smoke HTTP thật bằng credentials dev: login 302 dashboard → `POST /admin/posts/save` `intent=draft` title "Sm" content trống → **302 `flag=created`**, row `status=0` + draft tab hiện bài; `intent=publish` content trống → 200 re-render đúng "Nội dung bài viết không được để trống."; bản 500 cũ rollback sạch (0 orphan — xác nhận transaction hoạt động); mọi dòng test + file tạm đã xoá.
> **Mốc 13/09/2026 (giao diện Admin mới — port từ mockup):** theo yêu cầu user, port toàn bộ mockup `Image/files/{cms-doanh-nghiep.html, cms-style.css, cms-app.js}` (giữ nguyên bản gốc, không sửa) vào module Admin. **Asset:** `public/css/admin.css` = css mockup nguyên vẹn + mục "PHẦN BỔ SUNG" (rules phục vụ MVC: `.toolbar-form`, `.two-col(.wide-left)`, `.kv-table`, `.list-row`, `.upload-area` + input[file] ẩn trong label, `.settings-panel`, `.media-actions form{display:contents}`, `.avatar(.sm)`, `.tag-badge.purple`…); `public/js/admin.js` = js mockup chuyển SPA→MVC (tab bài viết/status thành link thật; settings tab chuyển panel trong MỘT form + tự nhảy tới panel có `.field-error`; editor toolbar bọc `data-open/close` quanh selection của textarea cùng `.form-group`; char-count cho `textarea[maxlength]`; th→td select-all; upload hiện tên file; bar-chart animation). **Layout:** `view/layout/admin.phtml` viết lại — sidebar 12 mục highlight theo đường dẫn hiện tại qua helper `serverUrl()` (PhpRenderer **không** có `getRequest()` — ghi chú trong file), topbar + identity `$this->adminIdentity` (shape AuthGuard), logout POST CSRF. **View:** port **22 template** (dashboard · post index/form · category · tag · service · banner · home-section · team · contact index/view · media index/edit/usages · setting · account) sang design-system mới — giữ **nguyên** mọi chức năng thật: hidden input `csrfHash()` mọi form, flag PRG→`alert-*`, GET filter/pagination, 3 nút intent (nháp/hẹn giờ/xuất bản), form merge tag, items FR-33 ↑↓/remove, đổi alt media, xử lý contact. `auth/login.phtml` **không đụng** (user đã tự làm). **Bootstrap rút khỏi Admin:** không còn dòng nào trong `module/Admin/view` tham chiếu `bootstrap.*` (grep sạch "SACH-bootstrap"); file `public/{css,js}/bootstrap*` chỉ còn bị nhắc ở `module/Application/view/layout/layout.phtml` — layout **thây ma** của skeleton Laminas, không layout/module nào thực dùng. Nghiệm thu: `php -l` 24/24 OK, phpcs **0 lỗi toàn bộ `module/Admin/view`** (mốc cũ "~74 lỗi tồn tại ở 4 file" hết hiệu lực do rewrite + `phpcbf`), phpunit **204/204**, psalm **90 ≤ baseline** (`--no-cache`, bằng mốc batch 9 — không đổi logic), harness render container-thật đủ 22 template + layout sạch (file tạm đã xoá), `php -S`: `/admin/login` 200, `/css/admin.css` + `/js/admin.js` 200, `/admin` chưa đăng nhập 302. Không có trang xác thực nào smoke qua HTTP (không có credentials, tôn trọng luật 1-dòng `users`) — đề nghị user đăng nhập xem trực quan.
> **Mốc 13/09/2026 (batch 10 — FR-02 danh sách tin /tin-tuc):** `Frontend\Service\PostListService` + `PostController::listAction` hết Khung: **12 bài/trang** (`FrontendConst::NEWS_PAGE_SIZE`) theo `?page=`, trang vượt giới hạn **clamp về trang cuối**; lọc danh mục qua **`?danh-muc=<slug>`** (spec §6.1 chưa chốt tên tham số — convention tiếng Việt cùng kiểu route; FR-05 tái sử dụng bằng truyền `[id, ...con]` vào `listPublishedPage`), **slug lạ/danh mục ẩn → bỏ lọc im lặng** (404 là việc FR-05); menu danh mục cache `CacheConst::KEY_CATEGORY_MENU` ('category-menu-v1') TTL 60s theo §7.2, **danh sách bài không cache** (key nổ theo trang×danh mục, ghi rõ trong docblock service); `CategoryService` create/update/delete forget `category-menu-v1` + `home-v1` — đóng lỗ batch 9 (khối `category_posts` đọc tên danh mục lúc dựng payload). `PostMapper` +`listPublishedPage/countPublished` cùng scope công khai; view `post/list.phtml` (select auto-submit + nút Lọc dự phòng, pager cửa sổ ±3 giữ `danh-muc`) + CSS `.news-*`/`.pager*` trong `style.css`. **Sửa bug latent lộ khi smoke có dữ liệu:** `->where(['id IN' => $ids])` — laminas-db KHÔNG parse key mảng kết hợp ra toán tử, sinh SQL `WHERE \`id\` \`IN\` IN (…)` hỏng (500 thật trên /tin-tuc seed 15 bài; Admin PostService list/detail cùng gọi method này, trước nay chưa nổ vì DB seed rỗng) — `CategoryMapper/TagMapper` chuyển `Where::in()`, grep sạch toàn repo (`MediaMapper` đã đúng khuôn). **Khuôn test mới cho mapper-read:** builder Select tách method public (`namesSelect/publicSelect/countPublishedSelect` — docblock ghi lý do public) + render `(new Sql($mockAdapter))->buildSqlString($select)` với platform Mysql driverless (subclass override `quoteValue`→`quoteTrustedValue` để khỏi notice) — 3 file SQL regression test, không đụng Driver/Statement/ResultSet. Nghiệm thu: phpunit **221/221** (+17 so mốc batch 9 — 8 `PostListServiceTest`, 1 `CategoryServiceTest` forget mới, 9 SQL test; repo có phiên song song chỉnh song song — luôn chạy lại làm chuẩn), phpcs sạch trên file đụng vào (sửa 2 lỗi đóng tag `?>` tồn dư `home/index.phtml` + `list.phtml`), psalm **82 ≤ 90** `--no-cache`, smoke `php -S` 8086 với seed tạm `fr02-*` (15 bài/4 danh mục, đã clean): trang 1 12 card · trang 2 3 card · lọc `fr02-the-gioi` 12+1 · danh mục ẩn `fr02-tat` bỏ lọc trả đủ · `?page=99` clamp · option loại danh mục inactive, con thụt đầu "— " · `laminascache-category-menu-v1.dat` sinh đúng. Bảng FR lật ✅ dòng FR-02.
> **Mốc 13/09/2026 (batch 11 — FR-03 chi tiết bài viết /tin-tuc/{slug}):** `PostMapper::findPublishedBySlug` + `Frontend\Service\PostDetailService` (AppInvokableFactory) dựng payload mảng thuần **đủ 8 khối §3.3.2**: banner, breadcrumb danh mục, tên tác giả (chỉ string — UserModel có passwordHash, cấm xuống view), ngày d/m/Y giờ VN, phút đọc, tag chips (`tagIdsForPost` + `TagMapper::listByIds`), bài liên quan §5.4 **không JOIN chéo** — `PostTagMapper::sharedTagCounts` (single-table) → `listPublishedByIds` + pool danh mục, sort điểm/ngày/id DESC cắt 4, reuse card `buildCards` (nay là public), chia sẻ FB/X/Email URL tuyệt đối, SEO headTitle/description/og fallback title/excerpt. Chi tiết **không cache** (§7.2 không liệt kê). `detailAction` null → `notFoundAction()` + `setTemplate('error/404')` tường minh (không có template `frontend/post/not-found`; `not_found_template` chỉ ăn cho route-404). View `post/detail.phtml` (content echo thô — đã Purifier lúc lưu, §7.3) + CSS `.article*`/`.breadcrumb`/`.tag-chip`/`.share-*`. Nghiệm thu: phpunit **227/227** (+9), phpcs sạch, psalm **82** = baseline `--no-cache`, smoke `php -S` seed `fr03-*`: 200 đủ khối + related đúng thứ tự suy ra từ điểm tag, draft/slug lạ 404 — seed/file tạm/cache đã dọn. Bảng FR lật ✅ dòng FR-03.
> **Mốc 13/09/2026 (batch 12 — FR-04 xem trước `?previewToken=` + noindex):** FE nối phần còn thiếu của dòng 🟡: `PostMapper::findBySlugPreviewToken(slug, token)` — `WHERE slug=? AND previewToken=? LIMIT 1` **bỏ qua scope status/publishedAt** (unique `uq_posts_preview_token` → ≤1 dòng; bind prepared; token rỗng chặn từ đầu không chạm DB), builder `slugPreviewTokenSelect` public cho SQL test render. `PostDetailService::detail(slug, token)` chỉ tra nhánh token khi lookup công khai MISS; `detailAction` nhận `?previewToken=` query (trim, GET không InputFilter). **NFR-SEO-5 nghĩa đen:** mọi request kèm token không rỗng ⇒ payload mang flag `noindex` ⇒ view `appendName('robots','noindex')` — kể cả bài đã publish (URL query là bản sao, tránh index trùng). Nghiệm thu: phpunit **233/233** (+6: 4 service + 2 SQL), phpcs sạch, psalm **82** = baseline `--no-cache`, smoke `php -S` 12/12 với 1 draft seed token cố định + bài publish thật (draft+token 200 noindex, token sai/thiếu/rỗng 404, published+token vẫn noindex, published sạch không robots, token đúng + slug lạ 404) — seed/server/file tạm đã dọn. Bảng FR lật ✅ dòng FR-04.
> **Mốc 13/09/2026 (batch 13 — FR-05 trang danh mục /danh-muc/{slug}):** `CategoryListService::page` + `CategoryController::viewAction` (hết stub — xoá 4 action không route) + `category/view.phtml`. Danh mục **tắt hoặc slug lạ → 404** (đúng dòng FR — khác /tin-tuc ở đó bỏ lọc im lặng); bài hiển thị = của danh mục + **con đang bật** theo §5.3 — mapper mới `CategoryMapper::activeChildIds` một bảng không JOIN (cây chặn 2 cấp ở FR-26 nên một lớp đủ), cha tắt thì con cũng không cứu được trang (404 trước), con tắt tự ẩn khỏi trang cha nhưng bài nó vẫn đọc trực tiếp được (§3.2). Phân trang 12 + clamp + card `buildCards` + pager nguyên khuôn FR-02 (link `/danh-muc/{slug}?page=N`); đầu trang breadcrumb + mô tả `.cat-desc`, meta fallback tên/mô tả §3.2; không cache (slug×trang). Nghiệm thu: phpunit **242/242** (+9), phpcs sạch, psalm **74 ≤ 82** `--no-cache` (file batch 13 0 lỗi scoped — số tụt thêm do phiên song song), smoke `php -S` seed `fr05-*` (3 danh mục + 14 bài): 18/18 — 13 bài/2 trang, con tắt + draft ẩn, clamp, 404 đủ hai nhánh — seed/server/file tạm đã dọn. Ghi nhận: DB dev đổi tên `vanlang_news`→`news_system` giữa phiên bởi phiên song song (global.php; không ai hardcode tên cũ trong docs). Bảng FR lật ✅ dòng FR-05.
> **Mốc 13/09/2026 (batch 14 — FR-06 trang tag /tag/{slug}):** `TagListService::page` + `TagController::viewAction` (hết stub) + `tag/view.phtml` — card/pager nguyên khuôn FR-02/05. Ghép đúng luật 1 bảng 1 mapper: `PostTagMapper::postIdsByTag` đưa id vào **một** query của PostMapper qua cặp mới `listPublishedPageByIds`/`countPublishedByIds` (publicScope + Where::in('id',…) thật — không quay lại bug `['id IN' =>]`; tập rỗng chặn trước DB). **404:** tag lạ → 404; tag có nhưng 0 bài → 200 rỗng (tags không có bật/tắt như danh mục). Không cache. Nghiệm thu: phpunit **250/250** (+8: 6 service + 2 SQL), phpcs sạch, psalm **69 ≤ 74** `--no-cache` (file batch 14 0 lỗi scoped), smoke `php -S` seed `fr06-*` (tag + 13 bài publish + 1 draft): 14/14 — 12 card · draft ẩn · clamp · tag rỗng 200 · lạ 404 — seed/server/file tạm đã dọn. Bảng FR lật ✅ dòng FR-06.
> **Mốc 13/09/2026 (batch 15 — FR-07 tìm kiếm bài viết /tim-kiem?q=):** `SearchService::search` + `SearchController::indexAction` (hết stub — xoá 4 action không route) + `search/index.phtml`. Tìm kiếm toàn văn tiếng Việt sử dụng index FULLTEXT `ft_posts_title_excerpt` qua `PostMapper::searchPublished` (`MATCH(title, excerpt) AGAINST (:q IN NATURAL LANGUAGE MODE)` + publicScope `status=PUBLISHED AND publishedAt <= UTC_TIMESTAMP()` + ORDER BY `score DESC, publishedAt DESC, id DESC` + `LIMIT 20` theo §5.11/§4). Từ khoá rỗng hoặc `< 2` ký tự (theo `innodb_ft_min_token_size = 2`) chặn trước DB trả mảng rỗng; card reuse `PostListService::buildCards` (chống N+1). **NFR-SEO-5:** luôn gắn `<meta name="robots" content="noindex">`. Không cache. Nghiệm thu: phpunit **255/255** (+5: 3 service + 2 SQL render `PostMapperSearchSqlTest`), phpcs sạch, psalm **72 ≤ 80** `--no-cache` (file batch 15 0 lỗi scoped). Bảng FR lật ✅ dòng FR-07.
> **Mốc 13/09/2026 (rà giao diện — gỡ mọi control không có tính năng):** theo yêu cầu user, quét toàn bộ layout + 22 template Admin + login + `admin.js`, đối chiếu từng control với controller/JS handler thật. **Giữ** (xác minh có backend): toolbar bài viết `q/categoryId/tagId/featured/tab` + phân trang (`PostListFilter` khai đủ), toolbar Liên hệ `status/serviceId/dateFrom/dateTo` (`ContactService::listFiltered` đọc đủ), editor toolbar/char-count/settings-tab/upload-filename/sidebar-toggle. **Gỡ:** (1) dashboard — thẻ "Lượt xem 7 ngày", biểu đồ 14 ngày, card "Top bài xem nhiều" (nguồn FR-41 ❌ → luôn 0); `DashboardService::summary()` còn 4 số liệu thật (`postTabs/newContacts/processingContacts/mediaTotal`), xoá constants + `daysAgoUtc` + accessor `PostViewDailyMapper`; theo precedent "hết caller thì xoá": `PostViewDailyMapper::topPosts/viewsByDate` (mapper còn mỗi `deleteByPostId` — PostService gọi khi xoá bài cứng) và `PostMapper::mapTitlesByIds`; `DashboardServiceTest` 6→2 test. (2) `post/index.phtml` — cột "Xem" (`$row->viewCount` bao giờ cũng 0; colspan 6→5). (3) `admin.js` — 3 dead handler không phần tử nào trong view khớp: select-all `th input[type=checkbox]` (bulk FR-22 chưa có), tab lọc media `.media-tab[data-kind]` (trang media không có tab), animation `.bar` (chart đã gỡ). Cột `viewCount` + bảng `post_view_daily` GIỮ nguyên (schema thật — chờ FR-41; ghi chú vào dòng FR-41 bảng FR + luật 07 §5.1 tổng quát "cấm key mảng kèm toán tử" sau ca `id IN` của batch 10). Nghiệm thu: phpunit **217/217** (221 − 4 test widget), phpcs sạch file đụng vào, psalm `--no-cache` không tăng (82), HTTP smoke: `/admin/dashboard` đủ 6 thẻ stat + 0 chart · `/admin/posts` hết cột Xem · `/admin/media` 200.
> **Mốc 13/09/2026 (dashboard Tổng quan dựng LẠI y hệt mockup — thay mốc "rà giao diện" cho riêng trang này):** theo yêu cầu user "trang tổng quan phải giống y hệt demo", `dashboard/index.phtml` dựng đúng khuôn `#page-dashboard` của `Image/files/cms-doanh-nghiep.html`: page-header subtitle "Chào mừng bạn trở lại…", **4 thẻ stat** (Tổng bài viết / Danh mục / Dịch vụ / Liên hệ mới — icon SVG + màu nguyên văn mockup, `div` thường không còn là link), **dashboard-grid** = card "Bài viết theo tháng" (bar-chart 12 cột T1–T12, bar tháng hiện tại `active`, chiều cao theo % số bài xuất bản nhiều nhất tháng) + card "Bài viết mới nhất" (3 bài, thumb placeholder 3 màu luân phiên đúng mockup, `tag-badge` tên chuyên mục cyan/green/purple, "Xem tất cả →" về `/admin/posts`). **Toàn bộ số liệu là THẬT** — không widget lượt xem (FR-41 vẫn chưa có nguồn; mockup cũng không có). `DashboardService::summary()` đổi payload (`postTotal/categoryTotal/serviceTotal/newContacts` + 4 `*Delta` "so với tháng trước" + `monthlyPosts` 12 ô + `currentMonth` + `recentPosts`); delta + đếm tháng theo lịch **giờ VN**, mốc UTC suy ra từ đó. Mapper mới mỗi-bảng-một-method (07 §5): `PostMapper::{countCreatedBetween,countPublishedByMonth}` (SELECT tháng + `Predicate\Operator` bind mốc năm, không nội suy), `CategoryMapper::{countAll,countCreatedBetween}`, `ServiceMapper::{countAll,countCreatedBetween}`, `ContactMapper::countByStatusBetween`; **xoá** `MediaMapper::countAll` + `ContactMapper::countByStatus(processing)` khỏi payload (hết caller — precedent "hết caller thì xoá"; `countByStatus` giữ, card "Liên hệ mới" vẫn dùng). `processingContacts`/`mediaTotal`/`postTabs` không còn ở view. Test: `DashboardServiceTest` 2→3 test (shape payload, delta từ mapper chủ, recent ghép tên danh mục). Nghiệm thu: phpunit **227/227**, phpcs sạch file đụng vào, psalm `--no-cache` **85** (0 lỗi trên code mới; repo chỉnh song song nên lệch mốc 82 — không tăng do batch này), HTTP + browser smoke thật: `/admin/dashboard` 200, screenshot đối chiếu `cms-doanh-nghiep.html` (serve tạm `php -S :8090` qua `Image/files`) — layout 1:1, khác mỗi giá trị số (thật vs demo).
> **Mốc 13/09/2026 (batch 16 — FR-08 trang dịch vụ /dich-vu + /dich-vu/{slug}):** tầng đọc công khai mới `Frontend\Service\ServiceViewService` (extends AppServiceFactory — đăng ký `AppInvokableFactory`): `list()` = toàn bộ dịch vụ active (`isActive=1`, `sortOrder ASC, id ASC`) với icon/ảnh resolve **một** query qua `MediaMapper::mapCardsByIds` (chống N+1 — khuôn batch 9/10); `detail(slug)` = `findActiveBySlug` — slug lạ HOẶC dịch vụ tắt → null → `notFoundAction()` + `setTemplate('error/404')` khuôn batch 11; meta fallback `name`/`shortDescription` (§3.5). `ServiceMapper` (bảng do Frontend sở hữu — 05-cau-truc §4) thêm cặp mới `listActiveAll`/`listActiveAllSelect` + `findActiveBySlug`/`activeBySlugSelect` (Where `slug + isActive` + `LIMIT 1`, bind prepared; slug rỗng chặn trước DB) — builder tách public theo khuôn SQL-test batch 10. `ServiceController` rewrite mỏng: `listAction`/`detailAction` + closure DI; xoá stub cũ (index/view/submit không route nào trỏ tới). View `service/list.phtml` (breadcrumb Trang chủ › Dịch vụ + `.service-card` grid, link `/dich-vu/{slug}`) + `service/detail.phtml` (breadcrumb 3 cấp + lede + `.article-body` echo THÔ content — đã Purifier lúc lưu §7.3 — + `headTitle`/description fallback); CSS block FR-08 trong `style.css`. Không cache (bảng ít bản ghi, §7.2 không liệt kê — docblock). FR-09 (batch 17) code song song trong cùng chuỗi phiên — nghiệm thu chạy gộp, xem dòng kế.
> **Mốc 13/09/2026 (batch 17 — FR-09 trang đội ngũ /doi-ngu, luật ẩn email/SĐT):** tầng đọc `Frontend\Service\TeamViewService::list()` — toàn bộ thành viên active (`isActive=1`, `sortOrder ASC, id ASC`) qua **method mới `TeamMemberMapper::listAllActive`** trên mapper Admin-sở-hữu (precedent batch 9: Frontend đọc qua container gộp, chỉ SELECT); avatar qua `mapCardsByIds` chống N+1. **Luật §3.8 chốt ở tầng Service, không tin view:** `showContact=0` → payload trả `email`/`phone` = `null` (hằng mới `TeamMemberConst::SHOW_CONTACT/HIDE_CONTACT`) — view chỉ render link `mailto:`/`tel:` khi giá trị khác rỗng nên template không thể làm lộ dữ liệu. `TeamController` rewrite mỏng + closure DI (xoá stub cũ index/detail/view/submit); route `team` `/doi-ngu` default action `list` có sẵn. View `team/list.phtml`: breadcrumb + `.team-grid` cards (avatar `mediaUrl(thumb)` — thiếu avatar fallback **chữ cái đầu** tên `.team-avatar-placeholder`, `mb_strtoupper` an toàn UTF-8 — tên · chức vụ · bio · contact); CSS block FR-09. Không cache (§7.2). Nghiệm thu gộp batch 16+17 (chạy tại thời điểm ghi docs): phpunit **273/273** (batch 16 +8 — 5 `ServiceViewServiceTest`: list rỗng · list resolve media + items · slug rỗng → null · slug lạ → null · payload đủ + meta fallback; 3 `ServiceMapperSqlTest`: WHERE/ORDER list · slug+isActive+LIMIT · slug rỗng không chạm DB; batch 17 +6 `TeamViewServiceTest`: payload rỗng · resolve avatar · showContact=1 giữ email/phone · =0 → null · dedupe id avatar một query · cờ isFeatured; +4 test từ phiên song song khác — mốc batch 15 là 255), phpcs sạch trên file đụng vào, psalm **42 ≤ 72** `--no-cache` (file batch 16/17 scoped **0 lỗi** — tụt thêm do chỉnh sửa song song; 2 lỗi `MixedAssignment $flagRaw` nhắc tới là stub controller Admin tồn dư cũ). Bảng FR lật ✅ dòng FR-08 + FR-09.
> **Mốc 13/09/2026 (UI Admin theo feedback user — selectField thay ô nhập ID + cụm nút media gọn):** hai việc. (1) **Form không còn bắt gõ ID**: helper mới `Application\View\Helper\SelectField` (đăng ký `view_helpers` alias `selectField` — đổi config này nhớ `clear-config-cache`; không extends `AbstractHelper` đang deprecated, escape `htmlspecialchars` trực tiếp) render select box cho **mọi** field tham chiếu bản ghi: 7 ô media-id (banner ×2 · post banner+thumbnail · category cover · service icon+image · team avatar) + ô `avatarMediaId` trang **Tài khoản** (FR-14 — ô number sót cuối, phát hiện khi rà lại toàn bộ view; alert "media không tồn tại" giữ nguyên) + `itemId` form thêm mục home-section. Giá trị submit vẫn là id → **Filter/Service không đổi**; nguồn options theo chuẩn mới **07 §9**: `listOptions()` mỗi Mapper (`PostMapper` · `TeamMemberMapper` · `MediaMapper` mới; `ServiceMapper::listActiveOptions` tái dùng — phép chiếu scalar §5.1, order id DESC / sortOrder), Service cung cấp `mediaOptions()`/`itemOptions()` (avatar qua accessor `mediaMapper()` container gộp); option media gắn `data-src` + img `.media-pick-preview` (admin.js đổi ảnh khi chọn; id đang lưu không còn trong danh sách → option fallback, không mất giá trị). (2) **3 nút dưới mỗi thẻ ảnh thư viện Media** (Sửa/Đang dùng/Xoá — chữ dài tràn card 150px, user gọi là "nút dưới ảnh banner") chuyển thành **3 nút icon** 34×28 (bút/mắt/thùng rác SVG, `title` + `aria-label` giữ nhãn tiếng Việt; JS confirm + form CSRF xoá giữ nguyên). Nghiệm thu (chạy gộp song song batch 16/17): phpunit **273/273** (+4 `SelectFieldTest` — render selected/preview/fallback/escape), phpcs sạch file đụng vào, psalm `--no-cache` **42** (code mới 0 lỗi — 13 lỗi phát sinh của `SelectField`/`HomeSectionService` dọn ngay trong phiên), smoke render helper bằng `php -r` đúng + HTTP `/` 200 · `/admin/login` 200 · `/admin` `/admin/media` 302 guard. Các form sau login **chưa kiểm trực quan qua HTTP** (không credentials — luật 1 dòng `users`) — đề nghị user đăng nhập xem thử.
> **Mốc 13/09/2026 (batch 18 — FR-11 sitemap.xml tự sinh + cache):** tầng đọc mới `Frontend\Service\SitemapService::urls()` (extends AppServiceFactory, đăng ký `AppInvokableFactory`) = 5 đường dẫn tĩnh (/ · /tin-tuc · /dich-vu · /doi-ngu · /lien-he) + bài `/tin-tuc/{slug}` + danh mục `/danh-muc/{slug}` + dịch vụ `/dich-vu/{slug}`; **không** `/tim-kiem` (noindex NFR-SEO-5 — §7.1). Ba phép chiếu scalar mới theo khuôn SQL-test batch 10: `PostMapper::sitemapPublished`(+Select — dùng `publicScope` nên nháp/hẹn-giờ-tương-lai/archived tự loại; `slug, updatedAt`, KHÔNG LIMIT), `CategoryMapper::sitemapActiveSlugs`(+Select) và `ServiceMapper::sitemapActiveSlugs`(+Select — `isActive=1`, sortOrder/id ASC; method riêng vì `listActive` bắt truyền limit) — 2 service tắt trả 404 nên không được lộ URL. Payload `{path,lastmod}` **không gắn host** → cache nguyên khối `remember('sitemap-v1')` TTL 60s (`CacheConst::KEY_SITEMAP`), host do `serverUrl()` lúc render; `SitemapController` rewrite mỏng: Content-Type XML sau guard `instanceof Laminas\Http\Response`, view `setTerminal(true)` (XML thuần, không layout), stub cũ list/detail/view/submit xoá; `sitemap/index.phtml` một block PHP dựng chuỗi `<urlset>`, lastmod cắt `YYYY-MM-DD`. **Hook forget FR-39 mở rộng:** `invalidateHome()` của Admin `PostService` (5 điểm ghi) + `ServiceService` (3) đổi tên `invalidatePublicCaches()` forget thêm `sitemap-v1`; `CategoryService` cũng gắn `sitemap-v1` (khuôn instanceof-guard batch 9). Nghiệm thu: phpunit **280/280** (+7 — 4 `SitemapServiceTest`: tĩnh trước · lastmod W3C/rỗng · nối slug danh mục+dịch vụ · hit cache không gọi mapper lần 2; 3 SQL render mới — `PostMapperSitemapSqlTest` + append `CategoryMapperSqlTest`/`ServiceMapperSqlTest`; 3 test forget Admin đổi sang recording-callback đếm đúng 6/6/9 removeItem, unique có `sitemap-v1`), phpcs **18 file sạch 0 lỗi**, psalm `--no-cache` **34** ≤ baseline 72 (file batch 18 scoped 0 lỗi — 7 lỗi vừa sinh đã dọn trong phiên), smoke qua `php -S` + router tạm (⚠ `composer serve` vẫn 404 request `.xml` — ghi ở checklist): HTTP 200 `application/xml`, well-formed **29 URL** (5 tĩnh + 10 bài + 8 danh mục + 6 dịch vụ — khớp DB thật: 3 nháp/1 hẹn giờ/1 archived bị loại, không `/tim-kiem`), `laminascache-sitemap-v1.dat` tự dựng lại sau khi xoá tay. FR-11 lật ✅.

> **Mốc 14/09/2026 (MediaPicker gọn mắt theo feedback — ẩn "Chưa chọn ảnh", chọn xong tự hiện):** user chê UI field ảnh trong form ("ảnh sửa lại xấu quá"): bỏ hẳn dòng trạng thái `— Chưa chọn ảnh —`/`Ảnh hiện tại: …` khỏi `Application\View\Helper\MediaPicker` — mỗi field giờ là **khung preview cố định 200×132** (`.media-pick-frame` nét đứt, icon camera nhạt khi trống, **click vào khung = mở popup thư viện** như nút chọn; Enter/Space hỗ trợ), chọn ảnh xong **ảnh tự hiện lấp đầy khung không nhảy layout**; 2 nút "Chọn ảnh từ thư viện…" + "Bỏ chọn ảnh" gom vào MỘT hàng `.media-pick-actions` (flex, clear hidden khi trống); riêng id trỏ media đã bị xoá vẫn còn cảnh báo đỏ `.media-pick-error` (mất sau thao tác đầu). admin.js: `mediaPickRender` viết lại theo hidden-attr (không dùng src="" nữa), thêm loop khung click. **Cập nhật cùng mốc theo feedback tiếp theo:** bỏ hẳn nút "Bỏ chọn ảnh" dạng chữ — thay bằng **dấu × tròn nhỏ overlay góc phải-trên khung ảnh** (`.media-pick-clear`, `aria-label="Bỏ chọn ảnh"`, hover đỏ, chỉ hiện khi đang có ảnh chọn; click × không mở popup — handler khung bỏ qua target trong `.media-pick-clear`). Helper luôn render cả `img` + `span.media-pick-empty` (kèm hidden đúng chiều) để JS trả lại trạng thái rỗng sau bấm ×. **Cập nhật tiếp cùng mốc (feedback "bỏ luôn nút chọn ảnh từ thư viện đi"):** nút chữ "Chọn ảnh từ thư viện…" cũng bị BỎ hẳn — hàng `.media-pick-actions` biến mất khỏi helper lẫn `admin.css` (kèm rule `.media-pick-btn`), **khung preview trở thành trigger chọn duy nhất**: khung mang lại `role=button` + `tabindex=0` (phương án trung gian "bỏ role/tabindex để keyboard đi qua nút" hết hiệu lực), click hoặc Enter/Space mở popup qua `mediaPickOpen(group)` trong admin.js gọi trực tiếp `rteOpenMediaModal` (không còn forward `btn.click()`); hover/`:focus-visible` đổi viền sang primary. Field media giờ chỉ còn ĐÚNG một khối nhìn thấy được: khung ảnh + × góc khung. 9 field media trên 6 form (team/banner/account/post×2/category/service×2) dùng chung khuôn — không đổi gì tầng Filter/Service (vẫn hidden input id). Dọn luôn 3 `RedundantCastGivenDocblockType` tồn tại trong file helper. Nghiệm thu: harness check helper 12/12 PASS (3 trạng thái trống/chọn/id chết + dedupe lib JSON), phpunit toàn suite giữ **329/330** (RED duy nhất vẫn `SelectFieldTest` — work-in-progress phiên song song trên `SelectField.php`/`MediaPicker.php`, không liên quan batch này), phpcs sạch, psalm scope `MediaPicker.php` **0 lỗi** (trước đó 3).

> **Mốc 14/09/2026 (thumb ảnh + lightbox ở danh sách đội ngũ Admin):** theo feedback user, cột "Ảnh" trang `/admin/team` từ chỗ chỉ hiện vòng chữ cái đầu + `#mediaId` thành **thumbnail nhỏ 48px** (biến thể `thumb`), **click mở lightbox ảnh to** (biến thể `large`, không có thì file gốc — đóng trong trang, Esc/×/click nền để thoả, không sang tab mới). Tầng dữ liệu theo đúng luồng 07 §2: `TeamMemberService::avatarCardsFor()` gom id avatar distinct rồi gọi **một** query `MediaMapper::mapCardsByIds` (chống N+1 — Service điều phối mapper bảng khác, không JOIN; media bị xoá → không có trong map → view fallback vòng chữ cái + link `#id` như cũ); `mapCardsByIds` mở rộng shape thêm khóa `large` (additive — 4 docblock consumer Frontend `HomeService::imageOf`/`ServiceViewService::detail` cập nhật theo trong cùng phiên). `TeamController::indexAction` truyền thêm `avatars` cho view. Nền UI tái dùng khuôn `rte-modal`: `.thumb-zoom`/`.img-lightbox` trong `admin.css` + delegated listener `[data-lightbox]` cuối `admin.js`. Nghiệm thu: phpunit toàn bộ suite **329/330** — toàn bộ test batch này + phần còn lại xanh, RED duy nhất là `SelectFieldTest` **không liên quan batch** (giải thích ⚠ bên dưới; batch có +2 test `avatarCardsFor` — dedupe một query · danh sách trống gửi `[]`), phpcs sạch file đụng vào, psalm file scope **0 lỗi mới** (7 lỗi báo trên `TeamController`/`TeamMemberService` là tồn dư suppress/MixedAssignment có từ trước), harness render template thật 6/6 PASS (thumb markup, data-full large, alt, fallback `—` và `#id` mất media). ⚠ Ghi nhận song song: `module/Application/src/View/Helper/SelectField.php` bị phiên khác sửa lúc 22:59 (bỏ `media-select`/`data-src`, helper mới `MediaPicker.php` chưa đồng bộ) khiến test của nó đỏ — file team/media của batch này không chạm helper đó, không tự sửa vì đang là work-in-progress của phiên khác; lần chạy full suite ngay trước 22:59 vẫn OK 330/330.

> **Mốc 14/09/2026 (rà Admin backlog — chốt widget lượt xem Dashboard):** rà toàn bộ đầu việc còn của Admin sau khi bảng FR lật ✅ 42/42: không còn tính năng ❌/🟡. **User chốt: dashboard GIỮ NGUYÊN theo mockup — không dựng lại widget lượt xem**, kể cả khi FR-41 (14/09) đã có nguồn số liệu thật (`post_view_daily` + `posts.viewCount`). Sửa note cũ trong docblock `Admin\Service\DashboardService` (lý "FR-41 chưa có nguồn" đã hết hiệu lực → ghi đúng quyết định). Các mục còn lại ghi nhận là backlog có chủ đích, không làm: reorder tầng API ngoài categories (page-level đã đủ — docblock `ApiController`), `CLAUDE.md` từng module chưa viết (05-cau-truc), checklist test thủ công `04-huong-dan/03` chưa đánh dấu. Không đổi hành vi — chỉ docblock + docs; nghiệm thu: phpcs sạch trên file đụng vào, phpunit giữ baseline.
> **Mốc 16/09/2026 (vá cache bảng giá + dọn rác + đồng bộ baseline):** PR `pricing-v1-*` theo `md5(group|q)` khiến `Admin\PricingService::invalidatePublicCaches()` forget có một khóa gốc — bộ lọc đổi → trả dữ liệu cũ. **Frontend\Service\PricingViewService** đổi về **một** khóa `pricing-v1` cache toàn bộ list active (mảng thuần như `home-v1`); bộ lọc `nhom/q` áp dụng bằng PHP trên mảng đã cache — invalidate Admin một dòng là đủ (không còn variant thừa). TTL 60s giữ nguyên (global.php). Dọn **11 file tạm/work-in-progress** vướng CRLF trên host Windows + rác phiên song song: `data/cache/_verify_seed.php`, `bash.exe.stackdump`, `cookies.txt`, `_tmp_ck.txt`, `jar.txt`, `t1.html/t2.html`, `p2.html/acct.html`, `phpserver.log/phpserver-demo.log`, `nul` — `data/cache/page/laminascache-07|72` chứa variant cũ pricing/home. Baseline nghiệm thu cập nhật theo thực chạy ngày 16/09 (AGENTS.md §4 — báo lệch giảm/đỏ, không khớp cứng số): phpunit **334/334 (958 assertions)**, psalm **140 lỗi --no-cache** (chủ yếu stub controller + Unused/PossiblyUnused tồn dư), phpcs **0 lỗi trên file đụng vào** — 11 WARNING line-length là tồn dư. Không đổi schema/route/api.
> **Mốc 17/09/2026 (phân trang dịch vụ /dich-vu):** `GET /dich-vu?page=N` lưới phẳng 12/trang (FR-08 mở rộng — giữ không cache như FR-02/05/06, index `isActive+sortOrder` đã đủ): `FrontendConst::SERVICE_PAGE_SIZE=12` (tách khỏi `NEWS_PAGE_SIZE` để rõ nghĩa) + `ServiceMapper::countActive`/`countActiveSelect` + `listActivePage(12,offset)`/`listActivePageSelect` (`LIMIT/OFFSET`, `ORDER sortOrder ASC, id ASC` — builder tách public cho SQL-test như batch 10) + `ServiceViewService::paginate(page)` (`ceil(total/12)`, kẹp `min(max(1,page),max(1,pages))`, `mapCardsByIds` dedupe một query như `list()` cũ; giữ `list()` wrapper cho `grouped()` fallback DB cũ) + `ServiceController::listAction` đọc `?page=` (Literal route `/dich-vu` giữ nguyên, không đổi route) + `service/list.phtml` rewrite phẳng `services-page-grid` + `news-count` + pager `±PAGER_WINDOW` ellipsis (copy `post/list.phtml`, `pageUrl=/dich-vu?page=N`) + pricing rút gọn 10 dòng + CTA giữ nguyên; bảng FR 01-yeu-cau-chuc-nang cập nhật FR-08. CSS `.pager` tái dùng — không CSS mới.

## Đọc gì trước?

| Thứ tự | File | Khi nào đọc |
|--------|------|-------------|
| 1 | `00-tong-quan/01-huong-dan-su-dung.md` | Lần đầu vào dự án |
| 2 | `00-tong-quan/02-tong-quan-du-an.md` | Hiểu dự án làm gì |
| 3 | `00-tong-quan/03-kien-truc-tong-quan.md` | Hiểu luồng & kiến trúc |
| 4 | `01-quy-chuan/00-readme.md` | Trước khi viết dòng code đầu tiên |

## Cấu trúc

```
docs/docs-dev/
├── README.md                    ← bạn đang ở đây
├── 00-tong-quan/                ← Bức tranh toàn cảnh (đọc trước)
├── 01-quy-chuan/                ← Quy chuẩn code (bắt buộc tuân thủ)
│   ├── 04-checklist-review.md   ← Checklist trước khi gửi review
│   ├── 05-quy-chuan-api.md · 06-quy-uoc-const.md  ← API `/api/admin/*` · const theo entity
│   ├── 07-crud-convention.md    ← NGUỒN SỰ THẬT cấu trúc tầng module (Controller→Service→Model→Filter)
│   └── 08-vi-du-sai-dung.md     ← Sai ↔ Đúng: 4 luật thực thi tầng + ánh xạ khuôn EnglishTrain → News
├── 02-thiet-ke/                 ← Yêu cầu & thiết kế
├── 03-tich-hop/                 ← Tích hợp bên ngoài
├── 04-huong-dan/                ← Hướng dẫn phát triển
├── 05-van-hanh/                 ← Vận hành & deploy
└── _templates/                  ← Mẫu copy khi thêm mới
```

## Cách dùng cho dự án mới

1. Copy toàn bộ `docs/docs-dev` sang repo mới.
2. Điền `00-tong-quan/02-tong-quan-du-an.md` và `05-van-hanh/01-cai-dat-moi-truong.md` trước.
3. Mỗi khi thêm tính năng: copy `_templates/template-tinh-nang.md` → `02-thiet-ke/xx-ten.md`.
4. Mỗi khi thêm API ngoài: copy `_templates/template-api.md` → `03-tich-hop/xx-ten.md`.

## Quy ước đặt tên

- Thư mục đánh số `00-`, `01-` để sort đúng thứ tự.
- File đánh số `01-`, `02-` trong mỗi thư mục.
- Tên file `kebab-case`, tiếng Việt không dấu, mô tả đúng nội dung.
